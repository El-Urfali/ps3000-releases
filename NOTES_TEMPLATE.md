Written for the person pressing the button, not for the person who wrote the code.
Three lines at most — it renders inside a 600 px panel on a shop tablet.

Say what changed that they will notice, and anything they must do differently.
Do not list commit subjects.

Example:

  Conditioning now stops and tells you when the string stops converging,
  instead of running all 100 cycles regardless. The stored-log size shown
  in diagnostics was reading zero and now reads correctly.
